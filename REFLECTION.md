What did you learn from reading and commenting on this code? 
I gained a better comprehension and confidence of using higher order functions, and also learned to use ...new Set(names) to remove duplicates from an array. 

What was the most confusing part?
The most confusing part for me was the uniqueHobbies() function because I wasn't initally familiar with using ...new Set(names) to remove duplicates from an array, so the code was difficult to understand. I understand what the code itself was doing, I was just unsure how it was doing it, so it was confusing. 
